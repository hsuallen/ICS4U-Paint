class Option {
  int x;
  int y;
  color colour;
  String choice;

  Option(int x, int y, color colour, String description) {
    this.x = x;
    this.y = y;
    this.colour = colour;
    this.choice = choice;
  }
  
  void draw() {
    
//    text(this.choice, 
  }
}
